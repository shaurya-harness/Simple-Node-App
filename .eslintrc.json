// .eslintrc.json
{
  "extends": "eslint:recommended",
  "env": {
    "node": true,
    "jest": true
  },
  "rules": {
    "semi": ["error", "always"],
    "quotes": ["error", "single"],
    "comma-dangle": ["error", "never"]
  }
}